# LAYER_CONF_VERSION is increased each time build/conf/bblayers.conf
# changes incompatibly
LCONF_VERSION = "7"

BBPATH = "${TOPDIR}"
BBFILES ?= ""

BBLAYERS ?= " \
  ${LAYER_ROOT}/openembedded-core/meta \
  ${LAYER_ROOT}/meta-openembedded/meta-oe \
  ${LAYER_ROOT}/meta-arm/meta-arm-toolchain \
  ${LAYER_ROOT}/meta-arm/meta-arm \
  ${LAYER_ROOT}/meta-rockchip \
  ${LAYER_ROOT}/meta-muxnix \
"
